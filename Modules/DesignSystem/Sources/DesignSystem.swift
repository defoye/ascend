/// Namespace placeholder for the Ascend DesignSystem module.
///
/// DesignSystem has no dependencies on other Ascend modules. It provides
/// reusable SwiftUI components, colors, and typography for Features.
/// See docs/design/DESIGN_SPEC.md for the full token and component spec.
///
/// DesignSystem previews use small inline sample data (never `Domain` types
/// or `MockData` from `InMemoryStore`) to preserve the module's zero-dependency
/// boundary — see docs/ARCHITECTURE.md.
public enum DesignSystem {}
