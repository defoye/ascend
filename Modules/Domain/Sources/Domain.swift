import Foundation

/// Namespace placeholder for the Ascend Domain module.
///
/// Domain contains the core business types (Person, Engagement, VerifiedOutcome, etc.)
/// as described in docs/DATA_MODEL.md. It depends on Foundation only — see
/// docs/ARCHITECTURE.md for the dependency rule enforced across modules.
public enum Domain {}
